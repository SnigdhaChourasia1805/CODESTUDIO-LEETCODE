#include <bits/stdc++.h> 
int countCubeSumPairs(int n) {
    int count=0;
    for(int i=0;i<=n;i++){
        for(int j=0;i<=n;j++){
            if(n==pow(i,3)+pow(j,3)){
                count ++;
            }
        }
     return count;   
    }
}
